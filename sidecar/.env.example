# --- Auth ---
# Shared secret the Vercel /api/chat-full proxy sends in Authorization: Bearer <token>.
# Generate one: `openssl rand -hex 32`
SIDECAR_SHARED_SECRET=

# --- Anthropic ---
ANTHROPIC_API_KEY=

# --- Working directory the agent operates in ---
# Defaults to the research repo so the agent can read commits, eval results, etc.
EPS_WORKDIR=/home/thomasjiralerspong/explore-persona-space

# --- Bind ---
SIDECAR_HOST=127.0.0.1
SIDECAR_PORT=7654

# --- Per-request safety rails ---
SIDECAR_MAX_BUDGET_USD=1.00          # forwarded to claude --max-budget-usd
SIDECAR_TIMEOUT_S=300                # subprocess wall-clock cap
CLAUDE_PERMISSION_MODE=bypassPermissions

# --- Path to the claude binary (auto-detected if on PATH) ---
CLAUDE_BIN=/home/thomasjiralerspong/.local/bin/claude
